# .env (project root — used by docker-compose)
NODE_ENV=production
MONGO_USER=admin
MONGO_PASSWORD=StrongMongoPass!2024
MONGO_APP_PASSWORD=AppDbPass!2024

# Generate these with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_64_CHAR_HEX
JWT_REFRESH_SECRET=REPLACE_WITH_ANOTHER_64_CHAR_HEX

FRONTEND_URL=http://localhost
ADMIN_USER=mongoadmin
ADMIN_PASSWORD=MongoAdminPass!2024
